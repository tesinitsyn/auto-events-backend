spring:
  datasource:
    url: jdbc:postgresql://db-prod:5432/autoevents
    username: prod_user
    password: prod_password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: WARN
